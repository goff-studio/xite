# xite
- Clone the project
- `npm install` to install dependencies
- `npx pod-install` if you want to build in MacOS and text with iOS Simulator
- `npx react-native link` to link customized fonts
- and simple use `npm run android` to check the app on Windows/MacOS/Linux mashines, or `npm run ios` to test the app in MacOS mashines
